{
  "version": 2,
  "builds": [
    {
      "use": "@vercel/next",
      "config": {
        "outputDirectory": ".next/standalone"
      }
    }
  ],
  "routes": [
    { "src": "/api/**", "dest": "/api" },
    { "src": "/public/**", "dest": "/public" }
  ]
}
